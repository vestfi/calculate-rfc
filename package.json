{
  "name": "calculate-rfc",
  "repository": "git://github.com/vestfi/calculate-rfc.git",
  "version": "0.0.2",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": ">=6",
    "lint-staged": ">=10",
    "prettier": "^2.7.1",
    "typescript": "^4.5.4",
    "vite": "^2.9.9",
    "vite-plugin-dts": "^1.2.1",
    "vitest": "^0.16.0"
  },
  "lint-staged": {
    "*.{js,css,md,ts}": "prettier --write"
  }
}
